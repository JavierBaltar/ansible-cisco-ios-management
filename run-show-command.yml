---
  tasks:
  - name: Test Reachability to Ansible
    hosts: all
    ios_ping:
      dest: 172.29.29.12

  - name: Command
    hosts: all
    ios_command:
      commands:
        - show version
    register: version
    
  - debug: var=version.stdout_lines

 
